---
- name: Test Pod Graceful Termination
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Wait for 2 minutes to simulate a running job
      ansible.builtin.pause:
        minutes: 2
    
    - name: Confirm successful completion
      ansible.builtin.debug:
        msg: "Job completed successfully after the 2-minute pause!"
